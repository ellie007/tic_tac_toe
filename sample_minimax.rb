class Ai

  def find_move(board, current_player)
    token = "X"


    available_spaces(board).each do |space|
      node = board.dup
      fill_space(node, space,

    take the root node
    find root node's available spaces
    fill each space of the root node's availble spaces and find the best move

    end
  end


end
